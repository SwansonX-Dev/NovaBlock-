<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.nova</groupId>
    <artifactId>novablock</artifactId>
    <version>0.48.5</version>
    <packaging>jar</packaging>
    <name>NovaBlock</name>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/releases/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- PINNED, do not turn this back into an open range. The live server is
             Purpur 26.1.2, whose paper-api ships Adventure 4.x. An open-ended range
             silently resolved to paper-api 26.2, which ships Adventure 5.x, and the
             two are NOT binary compatible: `ComponentBuilder<C>`'s type bound moved
             from BuildableComponent to Component, so every `builder.build()` we
             compile against 5.x emits a `build()Lnet/kyori/adventure/text/Component;`
             call that does not exist on 4.x — NoSuchMethodError at runtime, in code
             that compiled perfectly. Bump this deliberately when the server moves. -->
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>26.1.2.build.74-stable</version>
            <scope>provided</scope>
        </dependency>
        <!-- xEconomy native API. NovaBlock hard-depends on xEconomy for currency;
             the jar is installed alongside via xEconomy's `publishToMavenLocal`. -->
        <dependency>
            <groupId>dev.xsuite</groupId>
            <artifactId>xeconomy-api</artifactId>
            <version>0.4.10</version>
            <scope>provided</scope>
        </dependency>
        <!-- PlaceholderAPI – soft hook (PlaceholderHook is only registered when
             the PAPI plugin is present at runtime). -->
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>2.11.6</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.name}-${project.version}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
